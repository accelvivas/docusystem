<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="docusystem.Pages.RevisionHistory.RevisionHistoryPage"
             BackgroundColor="{StaticResource White}"
             Title="Proposal history">

    <ScrollView>
        <VerticalStackLayout Spacing="0">

            <!-- Navy Page Header -->
            <Grid BackgroundColor="#003087" Padding="18,16,18,22">
                <VerticalStackLayout Spacing="4">
                    <Label Text="Proposal history"
                           FontSize="20" FontAttributes="Bold" TextColor="White" />
                    <Label Text="View-only action trail for approvals, returns, resubmissions, and status changes."
                           FontSize="12" TextColor="White" Opacity="0.9" LineBreakMode="WordWrap" />
                    <Label x:Name="RevisionContextHintLabel"
                           Text=""
                           FontSize="11"
                           TextColor="White"
                           Opacity="0.88"
                           LineBreakMode="WordWrap"
                           Margin="0,4,0,0" />
                </VerticalStackLayout>
            </Grid>

            <VerticalStackLayout Padding="16,14,16,20" Spacing="12">

                <Border x:Name="LatestRevisionSummaryBorder"
                        BackgroundColor="{StaticResource PrimaryLight}"
                        Stroke="{StaticResource Primary}"
                        StrokeThickness="1"
                        StrokeShape="RoundRectangle 12"
                        Padding="12,10">
                    <VerticalStackLayout Spacing="4">
                        <Label Text="Latest actions (summary)"
                               FontSize="13" FontAttributes="Bold"
                               TextColor="{StaticResource Primary}" />
                        <Label x:Name="RevisionLatestSummaryLabel"
                               Text=""
                               FontSize="12"
                               TextColor="{StaticResource PrimaryDarkText}"
                               LineBreakMode="WordWrap" />
                    </VerticalStackLayout>
                </Border>

                <Border Style="{StaticResource CardBorder}">
                    <VerticalStackLayout Spacing="10">
                        <Label Text="Full action timeline" Style="{StaticResource SectionTitle}" />
                        <ActivityIndicator x:Name="RevisionLoadingIndicator"
                                           IsVisible="False"
                                           IsRunning="False"
                                           Color="{StaticResource Primary}"
                                           HorizontalOptions="Center" />
                        <Label x:Name="RevisionErrorStateLabel"
                               IsVisible="False"
                               FontSize="12"
                               TextColor="{StaticResource Primary}"
                               HorizontalTextAlignment="Center"
                               LineBreakMode="WordWrap" />
                        <VerticalStackLayout x:Name="RevisionTimelineStack" Spacing="10" />
                    </VerticalStackLayout>
                </Border>

            </VerticalStackLayout>
        </VerticalStackLayout>
    </ScrollView>

</ContentPage>
